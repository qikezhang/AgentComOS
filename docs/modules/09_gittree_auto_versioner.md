# GitTree and Auto Versioner

GitTree manages platform and capability assets, plus project code/config. Raw operating data is not versioned in Git.

Every medium/high risk release requires rollback_target.
