# Antigravity Task: OpenCode Runtime Manager

Implement:

- `opencode serve` health check
- `opencode run --attach` command builder
- session ledger
- stdout/json log capture
- failure/stalled detection
